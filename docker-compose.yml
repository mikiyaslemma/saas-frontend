version: '3.8'
services:
  saas-frontend:
    image: saas-frontend:latest
    container_name: saas-frontend
    ports:
      - "5173:5173"
    restart: unless-stopped
    volumes:
      - ./src:/usr/src/app/src
